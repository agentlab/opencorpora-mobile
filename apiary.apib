FORMAT: 1A
HOST: http://opencorpora.org/

# OpenCorporaAPI

Polls is a simple API allowing consumers to view polls and vote in them.

## Task Collection [/task/{type}&{count}&{complexity}]

### Pull tasks [GET]

+ Parameters
    + type (required, number, `1`) ... Тип задач, которые сервер должен отдать.
    + count (required, number, `10`) ... Количеств задач, которые нужна вытянуть с сервера.
    + complexity (optional, number, `5`) ... Желаемая сложность задач для решения.
    
+ Response 200 (application/json)

    + Attributes (object)
        + id: 123 (number)
        + type: "type_name" (string) - name of task type
        + complexity : 10 (number) - Complexity of tasks

            A positive integer between 1 and 100 that represents the discount the coupon will apply.

        + redeem_by (number) - Date after which the coupon can no longer be redeemed
        
        + Body 
        
            {
                "id": 123, 
                "type" : "noun", 
                "complexity": 5, 
                "text": "Solve it", 
                "choice": [
                    "noun",
                    ";dflgk",
                    "lkdjg"
                ]
            }


